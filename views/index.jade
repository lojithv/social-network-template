extends layout

block page 
  - var menu = 'index'
block content
  if (users.length === 0)
    | There's no published content yet. 
    a(href="/login") Log in
    |  to post and publish.
  else 
    each user, index in users
      div
        h2
          a(href="/#{user.name}")= user.name